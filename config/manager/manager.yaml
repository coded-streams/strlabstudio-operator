---
# config/manager/manager.yaml
# Deploys the operator controller into the cluster.
# Prerequisites:
#   kubectl apply -f config/crd/strlabstudios.yaml
#   kubectl apply -f config/rbac/rbac.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: strlabstudio-operator
  namespace: strlab-system
  labels:
    app: strlabstudio-operator
    control-plane: strlabstudio-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: strlabstudio-operator
  template:
    metadata:
      labels:
        app: strlabstudio-operator
        control-plane: strlabstudio-operator
    spec:
      serviceAccountName: strlabstudio-operator
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        fsGroup: 65534
      containers:
        - name: operator
          image: codedstreams/strlab-kube-operator:latest
          imagePullPolicy: Always
          command: ["kopf", "run", "/app/controllers/strlabstudio_controller.py", "--all-namespaces"]
          env:
            - name: OPERATOR_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu:    "50m"
              memory: "96Mi"
            limits:
              cpu:    "250m"
              memory: "256Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
      terminationGracePeriodSeconds: 30